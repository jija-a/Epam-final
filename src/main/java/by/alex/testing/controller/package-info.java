/**
 * Package contains all classes that represents controller layer.
 * The Controller layer handles requests from users.
 * It is responsible for rendering a response with
 * the aid of both the Model and the View layer.
 * <p>
 *
 * @since 1.0
 */
package by.alex.testing.controller;
